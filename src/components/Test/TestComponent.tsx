import React from 'react';

const TestComponent: React.FC = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>🎉 ¡Sistema de Login Funcionando!</h1>
      <p>Si ves este mensaje, significa que el sistema de autenticación está funcionando correctamente.</p>
    </div>
  );
};

export default TestComponent;